# 題目：感測器原始訊號正規化與異常波形過濾
背景：機台每秒會產生大量的溫度震盪訊號。這一次，我們不使用表格，而是直接處理原始的 數值陣列 (Array)。您需要對這些原始訊號進行「去噪」、「歸一化」以及「特徵提取」。
# 任務要求：
1. 資料預處理：建立一個包含 10 個數值的 1D 陣列，並將其中大於 100 的極端異常值直接「強制限制」在 100 以內。
2. 數據正規化 (Normalization)：將所有數值縮放到 0 到 1 之間。公式為：$$(x - min) / (max - min)$$。
3. 統計洞察：找出該組訊號中，高於「平均值」的數值共有幾個。
4. 矩陣重塑 (Reshaping)：將這組 10 個數據重塑為$$2 \times 5$$的矩陣，模擬兩組不同的感測器輸入。
# 初始 Code
```python 
import numpy as np

# 模擬原始感測器訊號 (包含極端異常值 150.0)
raw_signals = np.array([70.5, 72.1, 150.0, 68.9, 71.2, 95.5, 88.0, 75.3, 62.1, 70.0])

# --- 現在，請開始手寫以下任務 ---

# 任務 1: 使用 np.clip() 將數值限制在 [0, 100] 之間
# 任務 2: 執行正規化，計算出新陣列 signals_norm (提示：使用 signals.min() 與 .max())
# 任務 3: 找出高於平均值的數值個數 (提示：使用布林索引與 .sum())
# 任務 4: 使用 .reshape() 將陣列轉為 2x5 矩陣
```